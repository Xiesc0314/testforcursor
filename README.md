# 扫雷（Minesweeper）

本仓库包含一个**可直接在浏览器运行**的单文件扫雷游戏：`index.html`（无依赖）。

## 运行方式

- **最简单**：用浏览器直接打开 `index.html`
- **推荐**（本地起静态服务）：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`

## 操作

- **左键**：翻开格子
- **右键**：插旗/取消旗
- **双击已打开数字格**：当周围旗子数等于数字时，自动翻开周围未插旗格

